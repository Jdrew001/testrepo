/* typeahead.component.css */
.typeahead-container {
  position: relative;
  width: 100%;
}

.selected-value {
  margin-top: 5px;
  display: flex;
  flex-wrap: wrap;
}

.selection-chip {
  display: inline-flex;
  align-items: center;
  background-color: #e9ecef;
  border-radius: 4px;
  padding: 2px 8px;
  margin-right: 5px;
  margin-bottom: 5px;
  font-size: 0.875rem;
}

.clear-button {
  background: none;
  border: none;
  color: #6c757d;
  cursor: pointer;
  font-size: 1rem;
  line-height: 1;
  margin-left: 5px;
  padding: 0 2px;
}

.clear-button:hover {
  color: #343a40;
}

/* Make sure the input field's text is visible */
:host ::ng-deep .p-autocomplete .p-autocomplete-input {
  width: 100%;
}

/* Ensure dropdown reaches proper width */
:host ::ng-deep .p-autocomplete-panel {
  min-width: 100%;
}